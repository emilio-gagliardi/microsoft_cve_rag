{% extends "quarterly_deep_dive/llm_prompts/base_prompt.j2" %}

{# Specific context for the 'eop_by_attack_vector' chart insight #}
{% set output_format_description = "a concise text block (2-3 sentences) interpreting the key findings about the attack vectors used in EOP vulnerabilities" %}

{% block task_instructions %}
Analyze the chart image and data provided below for the chart titled '{{ data_context_payload.chart_caption | default('EOP by Attack Vector') }}'. This data shows the distribution of different attack vectors (e.g., Local, Network, Adjacent, Physical) specifically for Elevation of Privilege (EOP) vulnerabilities.
Identify the primary observations about how EOP vulnerabilities are typically exploited. Which attack vector is most common for EOPs? Are other vectors also significantly represented? Does this distribution suggest common scenarios leading to privilege escalation?
Describe the most significant findings regarding the attack vectors of EOP vulnerabilities in 2-3 sentences.
{% endblock task_instructions %}

{% block data_context %}
Chart ID: {{ data_context_payload.chart_id | default('eop_by_attack_vector') }}
Chart Caption: {{ data_context_payload.chart_caption | default('Figure 7. Distribution of Attack Vectors for EoP Vulnerabilities.') }}

Data Summary (Counts of EOP CVEs by Attack Vector):
{{ data_context_payload.chart_data_summary | default("Data not available.") }}
{% endblock data_context %}
