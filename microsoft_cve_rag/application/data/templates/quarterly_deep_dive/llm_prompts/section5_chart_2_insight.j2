{% extends "quarterly_deep_dive/llm_prompts/base_prompt.j2" %}

{# Specific context for the 'rce_cvss_distribution' chart insight #}
{% set output_format_description = "a concise text block (2-3 sentences) interpreting the key findings about the CVSS scores specifically for RCE vulnerabilities" %}

{% block task_instructions %}
Analyze the chart image and data provided below for the chart titled '{{ data_context_payload.chart_caption | default('RCE CVSS Distribution') }}'. This data shows the distribution (e.g., using violin or box plots) of CVSS base scores specifically for Remote Code Execution (RCE) vulnerabilities.
Identify the primary observations about the typical severity of RCEs. For example, what is the median or average CVSS score for RCEs? Are the scores tightly clustered in the high/critical range, or is there significant variation? Are there notable outliers?
Describe the most significant findings regarding the CVSS scores of RCE vulnerabilities in 2-3 sentences.
{% endblock task_instructions %}

{% block data_context %}
Chart ID: {{ data_context_payload.chart_id | default('rce_cvss_distribution') }}
Chart Caption: {{ data_context_payload.chart_caption | default('Figure 5. CVSS Base Score Distribution for RCE Vulnerabilities.') }}

Data Summary (CVSS Score Distribution for RCEs):
{{ data_context_payload.chart_data_summary | default("Data not available.") }}
{% endblock data_context %}
